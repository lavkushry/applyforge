## 2024-04-10 - Resolve N+1 query in Role Ingestion via _serialize_role Cache Mapping\n**Learning:** In `apps/api`, list endpoints utilizing `_serialize_[entity]` helpers (like `_serialize_role`) intrinsically trigger N+1 queries if relationships aren't eagerly loaded or cached. `_serialize_role` queries `TargetRoleSource` iteratively.\n**Action:** Add an optional `cache: dict | None = None` parameter to `_serialize_[entity]` functions. In list endpoints, pre-fetch relationships using `.in_()` and pass a `role_id` mapping into the serialization loop.

## 2024-05-10 - Form Component Accessibility Bindings
**Learning:** Form components in this app (like `auth-form.tsx`) often lack proper `htmlFor`/`id` bindings connecting labels to inputs, and missing `aria-describedby` links connecting inputs to validation error messages.
**Action:** When working on form components, proactively check for and add these accessibility bindings to improve screen reader support.

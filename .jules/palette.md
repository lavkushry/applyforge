## 2024-05-24 - Form Accessibility Bindings
**Learning:** Found that auth forms lack `htmlFor`/`id` bindings and `aria-*` attributes.
**Action:** Always link inputs to labels and error messages equipped with `role='alert'` to improve screen reader compatibility.

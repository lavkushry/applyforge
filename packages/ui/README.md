Reusable UI primitives and design tokens for ApplyForge web app.

The current MVP keeps UI primitives inside `apps/web/components/ui` for speed.
As the design system hardens, shared components can move here.
